@import "../../public/css/variables.pcss";

html, body {
  font-family: var(--text);
  overflow-y: hidden;
  height: 100vh;
  max-height: 100vh;
  min-height: 100vh;
}

.app {
  .wrapper {
    display: flex;
    height: 100%;

    .content {
      flex: 1;
    }
  }
}
