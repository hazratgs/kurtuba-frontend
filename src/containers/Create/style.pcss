@import "../../public/css/variables.pcss";

.create {
  background-color: #fff;
  padding: 36px 15px;

  .title {
    font-size: 16px;
    padding: 0;
    margin: 0 0 15px;
    font-family: var(--text);
  }

  .form {
    background-color: #fff;
  }
}

.loader {
  margin: auto;
  width: 24px;
  height: 24px;
  display: block;
  position: relative;
  left: 0;
  right: 0;
}
